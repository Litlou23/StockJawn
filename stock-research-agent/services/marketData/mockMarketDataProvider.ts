/**
 * DEPRECATED - mock market data is no longer used.
 * This file is intentionally empty. All market data comes from
 * Twelve Data (twelveDataProvider.ts). If the API key is not
 * configured, the system returns nulls with warnings instead
 * of fake data.
 */
